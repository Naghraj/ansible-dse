
stomp_interface: {% for node in groups['opscenter-node'] %}
{%- if dc_name == opscenter_dc -%}
{{ hostvars[node]["ansible_"~options.rpc_interface]['ipv4']['address'] }}
{% else -%}
{{ hostvars[node]["ansible_"~options.broadcast_interface]['ipv4']['address'] }}
{% endif %}
                 {% endfor %}
